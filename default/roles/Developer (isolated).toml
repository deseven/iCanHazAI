description = "Directory-isolated coding agent equipped with all essential tools for code generation."
prompt = "Developer (isolated)"
icon = "hammer.circle"
accent = "indigo"
connection_override_allowed = true
mcps_override_allowed = true
directory_isolation = true

[features]
with_attachments = true

[utils]
auto_allow_all = true

[filesystem]
auto_allow = ["ls", "read_file", "stat", "find_file", "find_text", "pwd"]

[code]
